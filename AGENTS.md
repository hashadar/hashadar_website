## Agent skills

### Issue tracker

GitHub Issues in `hashadar/hashadar_website`; external PRs are not a triage surface. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles mapped to matching GitHub label strings (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Issue Board

User project [hashadar.com Issue Board](https://github.com/users/hashadar/projects/3): Status columns + workflows. See `docs/agents/issue-board.md`.

### Domain docs

Multi-context — start at `CONTEXT-MAP.md`. See `docs/agents/domain.md`.

### Branching

Feature work targets **`develop`**; production is **`main`**; hotfixes branch from `main`. See `docs/BRANCHING.md`.
